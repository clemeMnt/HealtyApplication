<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HealtyApplication" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Aqua</item>
        <item name="colorPrimaryVariant">@color/Deepblue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Lightgrey</item>
        <item name="colorSecondaryVariant">@color/Red</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/Deepblue</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/Red</item>
    </style>



    <style name="Theme.HealtyApplication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextTitle" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/Deepblue</item>
    </style>

    <style name="SwitchModeCustom" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/Red</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#42221f1f
        </item>
    </style>

    <style name="Theme.HealtyApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.HealtyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>